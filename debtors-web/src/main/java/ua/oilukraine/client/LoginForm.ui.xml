<!--
Uncomment this for XHTML entities
<!DOCTYPE ui:UiBinder SYSTEM "http://dl.google.com/gwt/DTD/xhtml.ent">
-->
<ui:UiBinder xmlns:ui="urn:ui:com.google.gwt.uibinder"
             xmlns:gwt="urn:import:com.google.gwt.user.client.ui">
    <ui:style>
        .blackText {
        font-family: Arial, Sans-serif;
        color: #000000;
        font-size: 11px;
        text-align: left;
        }

        .redText {
        font-family: Arial, Sans-serif;
        color: #ff0000;
        font-size: 11px;
        text-align: left;
        }

        .loginButton {
        border: 1px solid #3399DD;
        color: #FFFFFF;
        background: #555555;
        font-size: 11px;
        font-weight: bold;
        margin: 0 5px 0 0;
        padding: 4px 10px 5px;
        text-shadow: 0 -1px 0 #3399DD;
        }

        .box {
        border: 1px solid #AACCEE;
        display: block;
        font-size: 12px;
        margin: 0 0 5px;
        padding: 3px;
        width: 203px;
        }

        .background {
        background-color: #999999;
        border: 1px none transparent;
        color: #000000;
        font-size: 11px;
        margin-left: -8px;
        margin-top: 5px;
        padding: 6px;
        }
    </ui:style>
    <gwt:HTMLPanel>
        <div align="center">
            <gwt:VerticalPanel styleName="{style.background}">
                <gwt:Label text="Логин" styleName="{style.blackText}" />
                <gwt:TextBox ui:field="loginBox" styleName="{style.box}" />
                <gwt:Label text="Пароль" styleName="{style.blackText}" />
                <gwt:PasswordTextBox ui:field="passwordBox" 
                                     styleName="{style.box}" />
                <gwt:HorizontalPanel verticalAlignment="middle">
                    <gwt:Button ui:field="buttonSubmit" text="Войти"
                                styleName="{style.loginButton}" />
                    <gwt:CheckBox ui:field="myCheckBox" />
                    <gwt:Label ui:field="myLabel" text="Запомнить"
                               styleName="{style.blackText}" />
                </gwt:HorizontalPanel>
                <gwt:Label ui:field="completionLabel1" 
                           styleName="{style.blackText}" />
                <gwt:Label ui:field="completionLabel2" 
                           styleName="{style.blackText}" />
            </gwt:VerticalPanel>
        </div>
    </gwt:HTMLPanel>
</ui:UiBinder>